---
title: Catte Music 情绪算法 v2 改进方案（2026-08-19）
type: Note
date: 2026-08-19T00:00:00.000Z
tags:
  - Catte Music
  - 情绪算法
  - 方案设计
  - 轻量化
  - 多标签分类
---

# Catte Music 情绪算法 v2 改进方案（2026-08-19）

基于《[[Catte Music 情绪算法解析（2026-08-19）]]》的现状与痛点（模板最近邻无学习、双体系不一致、特征丢失时序、置信度非概率），在**无 GPU、保持轻量化**的约束下，升级为**轻量多标签情绪表达**：输出从单一情绪变为 20 维情绪强度向量。

用户已确认的决策：目标 = 准确率 + 一致性；数据 = 弱标注 + 每情绪 10-30 首人工金标准；方案 = 方案 A（两段式轻量监督流水线）+ 多标签表达 + 旧数据自动迁移。

## 1. 核心变化：单标签 → 多标签情绪分布

- 输出从「1 个情绪」变为 **20 维情绪强度向量**（每类独立概率 0–1）；
- **主情绪** = argmax，兼容现有功能（分享徽章、推荐主标签、旧数据）；
- **次要情绪** = top-2/3 且概率 > 阈值（0.35）者；
- 示例：`{主: 治愈 0.82, 次: 宁静 0.61, 梦幻 0.44}` —— 一首歌可同时治愈+宁静+梦幻。

## 2. 架构

```
音频 → 特征增强 (~60维) → 多标签分类器 (OneVsRest HistGBDT ×20)
  → 20 维概率向量
  → 主情绪(argmax) + 次情绪(阈值筛选) + 全向量(推荐/展示)
  → 模板距离仍作特征 & 兜底
```

| 组件 | 变化 |
|---|---|
| `services/ai/feature.py` | 35 → ~60 维：+ delta MFCC 均值/std、频谱平坦度、能量熵、onset rate、spectral rolloff（全 CPU 毫秒级） |
| `services/ai/model.py`（新） | 多标签模型：OneVsRest + HistGradientBoosting ×20 二分类，每类后接 Platt 校准；predict → (主情绪, top-N, 20 维概率) |
| `services/ai/labeling.py`（新） | 弱标注聚合管线（见 §3），输出多标签训练集 |
| `services/ai/training_v2.py`（新） | 训练/评估脚本：分层 5-fold、宏 F1、每类 F1、Hamming loss，与模板分类器对比报告 |
| `api/analyze.py` | 推理切到模型，模型缺失/加载失败 → 模板最近邻兜底（现有逻辑原样保留） |
| `models/prediction.py` | `AiPrediction` 保留 `emotion_id`（主情绪）+ 新增 `emotion_probs` JSON（20 维向量）；旧数据无 probs 前端自动降级 |
| `api/emotions.py` | 返回 `{primary, top_emotions:[{name,color,prob}], probs(20维), radar(7维声学)}` |
| `services/recommend.py` | 情绪相似度从 7 维声学余弦 → **20 维情绪分布余弦**（更语义化；可与声学相似度加权 0.7/0.3） |

## 3. 多标签数据管线（CPU，离线）

- **弱标注源**（全自动，复用现有爬虫）：
  - B 站评论 `analyze_comment_emotions`（已是 {情绪:得分}）→ 阈值 0.3 转多标签；
  - Apple Music 编辑评论 `analyze_editorial_sentiment`（{情绪:得分}）→ 阈值 0.4 转多标签；
  - 网易云歌单标题/标签 → 单标签弱标注；
  - **仲裁**：同曲多源加权投票，冲突对（如治愈 vs 愤怒同时高分）不消除——多标签本就允许共存；仅单源且置信 < 0.5 的样本丢弃（降噪）。
- **金标准**：每情绪 10–30 首，人工勾选 1–4 个情绪 + 主情绪；训练时金标准样本权重 ×5；
- **规模**：从网易云热门歌单随机采样 300–500 首跑特征（CPU 分钟级）。

## 4. 训练与评估（CPU）

- OneVsRest HistGBDT：20 个二分类器、~500 样本，CPU 训练 **<1 分钟**；推理 20×几 ms ≈ **<50ms/首**；
- 每类 `CalibratedClassifierCV`（Platt）→ 概率真实可用；
- **验收标准**：宏 F1 显著高于模板最近邻基线；≥15/20 类 F1 ≥ 0.5；置信度校准后 Brier score 下降。

## 5. 兜底与降级链

1. 模型存在 → 20 维概率 → 主情绪 + top-N；
2. 模型缺失/加载失败 → 模板最近邻（现有逻辑，不崩）；
3. 训练数据不足（某类 <50 样本）→ 该类退回模板打分，模型输出与模板按类混合；
4. 主情绪概率 < 0.4 → 标记「情绪模糊」，前端只展示 top-3 弱表达，不硬给主标签。

## 6. 错误处理

- 模型文件缺失 → 兜底（模板），日志告警；
- 弱标注冲突仲裁后仍矛盾的样本 → 丢弃并计入统计日志；
- 特征提取失败（音频解析不了）→ 维持现有 400 语义。

## 7. 测试

- 单测：特征维度 35→60 一致性、多标签训练往返（合成小数据）、兜底路径、阈值筛选；
- 对比评估：同曲 20 首 模板 vs 模型（F1 + 人工盲评）；
- API 契约测试：`/emotion` 返回多标签结构，旧字段不破坏（前向兼容）。

## 8. 落地阶段

- **M1** 特征增强 + 多标签标注管线 + 数据采集脚本；
- **M2** 多标签模型训练/评估 + 与模板基线对比报告（未达标则回到 §5 混合降级策略）；
- **M3** 推理切换（模型+兜底）、DB/API 扩展、推荐相似度切换、前端多维展示（徽章多情绪 + 情绪分布条）、归档 RF 旧流水线。

## 9. 旧数据自动迁移

**目标**：存量 `AiPrediction`（无 `emotion_probs`）自动补齐 20 维情绪向量，幂等、可断点、无需人工。

**三级策略**：

1. **重新分析（首选）**：迁移命令逐首取试听音频 → 新 60 维特征 → 多标签模型 → 写入 `emotion_probs`，主情绪更新为模型 argmax，新旧对比记入迁移日志；
2. **模板近似（音频不可用）**：用存量 `feature_vector`（35 维）→ legacy 7 维 → 模板距离 → `softmax(−dist/τ)` 生成 20 维伪概率，`provenance=template`；
3. **保持 NULL（彻底失败）**：音频与特征均不可用 → 留 NULL，前端维持现有降级展示。

**实现要点**：

- 独立命令 `python -m app.services.ai.migrate --batch 50 --resume`：幂等（已有 probs 跳过）、id 游标断点续跑、每批输出 success/template_fallback/failed 统计；
- 不覆盖已有 `emotion_probs`（保护后续人工修正）；
- 部署后手动触发一次，此后新记录由 `analyze` 正常写入；
- 前端兼容：`emotion_probs = NULL` → 多维组件隐藏，降级单标签。

## 参考

- 《[[Catte Music 情绪算法解析（2026-08-19）]]》
- 《[[Catte Music 专利A — 音频情绪识别方法与模板库（申请文档草案）]]》
